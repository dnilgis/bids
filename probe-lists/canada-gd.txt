# GRAIN DISCOVERY SLUGS, CANADA — for scripts/gd-sweep.mjs --file, NOT --as-names
#
# Sig, 2026-09-06: "i say we add canadian elevators please".
#
# These are EXACT SLUGS, not company names, so run this list with the "treat
# each line as a company name" box OFF. The sweep already knows how to turn a
# name into slug variants; that is for when we do not have the slug. Here we do.
#
# WHERE THEY CAME FROM AND HOW GOOD THAT IS. Grain Discovery ships a separate
# white-label mobile app per customer, and the app listings name the customer
# and carry the slug. That is a published roster, not a guess at a URL — but it
# is a roster of CUSTOMERS, not proof that any given slug still answers
# /cash-bids publicly. The sweep is what settles that, one request each, and a
# 401 costs one request.
#
# TWO OF THESE ARE ALREADY OURS: addisgrain and sharedon. They are listed so a
# re-run measures them beside the rest rather than pretending they are new.
#
# DTN BOUGHT GRAIN DISCOVERY IN JULY 2025. Expect these endpoints to drift
# toward api.dtn.com over time. When a slug that used to answer stops, that is
# the likely reason and the DTN probe is the next place to look, not this file.
#
# EVERY BOARD BEHIND THESE IS IN CANADIAN DOLLARS. Nothing from this sweep gets
# enabled without country/currency set on its manifest — see lib/currency.mjs
# and the eight days Wanstead published CAD as USD.

addisgrain
sharedon
ansems
vdbgrains
clearview
dgglobal
rkm
mapleviewfarms
grantag
primatis
sevita
westlock
snobelen
mcdougall
grainsconnect
battleriver
rahr
huron
