# THE BUSHEL TEN — the biggest single win left in the queue.
#
# Ten operators, and SEVEN of them are CHS regions: one adapter, one shape,
# seven co-operatives. Measured by scripts/discover.mjs on 2026-08-20.
#
# What we know and what we do not: every one of these pages calls
#
#     https://api.bushelpowered.com/api/markets/aggregator/config/v1/GetMarketsConfig
#
# and gets 899-1100 bytes of JSON back. That is a CONFIG, not a board -- a board
# for a co-op with a dozen towns does not fit in nine hundred bytes. The config
# is almost certainly naming the request that does carry the board, and until
# 2026-08-20 discover reported it as a byte count and threw the body away.
# It now prints it. That is what this list is for.
#
# Cooperative Elevator also calls futures.bushelops.com/api/v1/futures (36 KB),
# which is a futures feed and not cash bids.
#
# NOTE FOR WHOEVER READS THE RESULT: Ace Ethanol's page carries Bushel's
# branding and a portal.bushelpowered.com link in its menu, and yet serves its
# board as plain server-rendered cashbidssingle HTML that our existing parser
# reads unchanged. "Bushel" is who powers the site, not necessarily how the
# board arrives. Check for a server-rendered table before writing an adapter.

#  READING LIVE, 2026-08-29:      https://www.chs-illinois.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsagservices.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chs-herman.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsdakotaplainsag.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsdrayton.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsfarmersalliance.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsriverplains.com/grain/cash-bids/
#  no bushel response in the ledger:  https://fscooperatives.com/gatewayfs/agriculture/cash-bids
#  moved to the 2026-08-29 block below, with the page the sweep measured:
#  READING LIVE, 2026-08-29:      https://www.michag.com/bids/

# ── SEVEN MORE CHS REGIONS, from the national sweep at start=400 (2026-08-26) ──
# Every one of these answered Bushel in a real browser -- not a guess off the
# markup, the feed itself was recognised. The adapter is written and already
# reading CHS Farmers Alliance live, so what stands between these pages and
# being source files is one probe run: which locations, under which ids, and
# which rounding rule each board's cash cell uses.
#
# chsprimeland and chssunbasingrowers answered on BOTH /cash-bids and /futures;
# the same board serves both pages, so probing one is enough.
#  moved to the 2026-08-29 block below, with the page the sweep measured:
#  READING LIVE, 2026-08-29:      https://www.chssunbasingrowers.com/grain/cash-bids
#  moved to the 2026-08-29 block below, with the page the sweep measured:
#  READING LIVE, 2026-08-29:      https://www.chshighplains.com/grain/cash-bids
#  READING LIVE, 2026-08-29:      https://www.chsholdrege.com/grain/cash-bids
#  READING LIVE, 2026-08-29:      https://www.chsunitedplainsag.com/grain/cash-bids

# ── NINE MORE CHS REGIONS, found 2026-08-26 ────────────────────────────────
# CHS has no public directory of its regional co-operatives -- chsinc.com/locations
# is a 404 -- so these were found a different way, and the method is worth keeping.
#
# Every CHS regional site is one WordPress multisite on a shared `chs-theme`. The
# rendered page is JavaScript-only and carries no platform string, but the REST
# API hands back the raw markup:
#
#     https://<region>.com/wp-json/wp/v2/pages?slug=cash-bids
#
# and `content.rendered` contains, verbatim:
#
#     <div id="cash-bids-root" data-locations="W10,W20,W30,...">
#     <script src=".../chs-theme/dist/js/bushel/cash-bids-front-end.DINZfkbf.js">
#
# `bushel` in the asset path, and THE SAME BUNDLE HASH on every site -- so it is
# provably the identical widget the fourteen regions we already read are served
# by, not a guess from a family resemblance. An empty data-locations array is a
# clean negative, which is how CHS Northwest was ruled out rather than assumed.
#
# 92 locations claimed across these nine.
#  READING LIVE, 2026-08-29:      https://chsag.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsdevilslake.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chs-texoma.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chssouthwestgrain.com/grain/cash-bids/
#  moved to the 2026-08-29 block below, with the page the sweep measured:
#  READING LIVE, 2026-08-29:      https://www.chsbrandon.com/grain/cash-bids/
#  READING LIVE, 2026-08-29:      https://www.chsfarmerselevator.com/grain/cash-bids/
#  moved to the 2026-08-29 block below, with the page the sweep measured:
#  READING LIVE, 2026-08-29:      https://www.chssouthcentral.com/grain/cash-bids/
#
# THREE THINGS TO KNOW BEFORE WRITING MANIFESTS FROM THE PROBE OUTPUT:
#
#  1. CHS ROCHESTER AND chsag.com OVERLAP. Rochester's bid points are
#     SAV,WINN,KASS,CTMN,OSTR,WYKO,SANS,MKTO,FMNT -- exactly the River Terminals
#     set minus COMS. Taking both double-counts nine elevators.
#  2. NOT LISTED, because they are probably aliases of regions we already read:
#     chsprairielakes.com is titled "Cash bids - CHS Herman", chsnortherngrain.com
#     is titled "Home - CHS Ag Services", and chselburn.com now serves CHS
#     Illinois. Confirm before adding any of them.
#  3. CONFIRMED NEGATIVE, do not build for it: CHS Northwest (chsnw.com) is nine
#     retail/agronomy/propane sites in WA and OR with an empty locations array.
#     CHS Larsen is historic -- merged into Country Visions in 2021.
#
# ONE REAL GAP: CHS West Central (the former West Central Ag Services, MN/ND,
# acquired January 2025) is on the same template, but its host failed TLS
# certificate verification on every attempt so no board could be confirmed.
# Worth one retry.

# ── THE LIST, REBUILT — 2026-08-29 ─────────────────────────────────────────
#
# The sharded sweep finished the country. The ledger now holds 40 hosts on
# Bushel: 19 already read live every hour, 4 are not customers, and these were
# the 17 left to probe.
#
# Every line above is commented out rather than deleted. A page that is now
# live is still the evidence for its own source file, and a probe run should
# not spend a browser load re-asking a board we read every hour.
#
# WHY DISCOVER COULD NOT ANSWER THIS. Its bushel signature keys on the LAST
# PATH SEGMENT of each response:
#
#     id: (u) => ({ endpoint: new URL(u).pathname.split("/").filter(Boolean).pop() })
#
# Across all 40 hosts that returned GetMarketsConfig 29 times, GetBidsList 24,
# futures 9, cash-bids 6 -- and modernizr, jquery.main.js and ScriptResource.axd
# besides. Endpoint names: the same strings for every Bushel operator alive.
# The locationId a source file needs is the location UUID INSIDE the GetBidsList
# body, and the sweep never opens the body. That is why platform_report.mjs
# counted all 40 as config rows and not one of them was.
#
# FOUR HOSTS ARE NOT CUSTOMERS and were deliberately absent:
#     portal.bushelpowered.com          Bushel own portal
#     bushelpowered.com/customers       Bushel own marketing page
#     agp.o.bushelsites.com             *** THIS WAS WRONG — see below ***
#     delphoscoop.dev.bushelsites.com   a DEV subdomain, not a production board
# Named rather than quietly dropped, so nobody re-adds them next month.
#
# ROUNDING IS THE REASON TO PROBE BEFORE WRITING ANYTHING. On 2026-08-25 six
# Bushel operators were enabled without measuring it and every one refused on
# cash - basis = futures: Ag-Land floors 80 rows of 80, CHS floors 12 of 12,
# and three others round BOTH displayed figures independently so no rule on
# one of them can ever reconcile the pair. Same evidence, three answers.
#
# The parser takes the first whitespace-delimited token per line, so the
# operator names are safe where they are.

# ── WHAT TWO RUNS MEASURED — 2026-08-29 ────────────────────────────────────
#
# Run 90064858519 asked ten of seventeen and was cancelled at a 20-minute
# timeout. Run 90133552278, with the timeout at 75 and the paths corrected,
# asked all fifteen and answered six: 19 locations.
#
# WHAT THE PATH FIX WAS WORTH. Nothing about those six sites changed between
# the runs except the URL on this list. A CHS front page loads
# .../chs-theme/dist/js/bushel/futures-front-end.<hash>.js -- the FUTURES
# widget; the board is on the cash-bids page and loads cash-bids-front-end.
# CHS Garrison in particular is /cash-bids/ and NOT /grain/cash-bids/, which
# 404s there: the CHS path is a family resemblance, not a rule.
#
# NINE OF THE NINETEEN ARE NEW. The other ten were already live, and the file
# said so before the run:
#   * chsrochester -- all EIGHT are chsag-* under another name. Note 1 above
#     called this exactly: "Rochester's bid points are SAV,WINN,KASS,CTMN,
#     OSTR,WYKO,SANS,MKTO,FMNT -- exactly the River Terminals set minus COMS.
#     Taking both double-counts nine elevators." It would have.
#   * chsprimeland -- already live as chsprimeland-chsprimeland.
#   * chsbigsky Shelby -- already live; Havre and Kershaw are new.
# Read the ledger against sources/ before writing a manifest, never the
# location count on its own.
#
# THE ROUNDING VERDICTS ON THE CANOLA BOARDS WERE WRONG AND ARE NOW FIXED.
# Garrison, all five SunPrairie locations, Havre, Kershaw and Kalispell all
# came back "NO RULE EXPLAINS IT" with residuals of -69522c, -73537c, -66040c.
# Every one of those boards quotes canola in USD/CWT against futures in another
# unit; the identity was never going to hold between the two printed numbers,
# and one such row in twenty condemned the whole board. roundingEvidence now
# sets those rows aside and says how many. Do NOT write cashRoundingCents from
# the first run's verdicts -- re-run and read the new ones.
#
# THE SECOND GENERATION IS HANDLED NOW, and that is what the dump box bought.
# Inco Grain came back COMPLETE at 2951 characters -- the whole document,
# closing meta and all -- and is fixtures/bushel-incograin-gen2.json. The
# branch was written from it. riceland, inco-grain and grower.coopelev all
# answered on futures.bushelops.com/api/v1/cash-bids and were refused only for
# want of that code. THEY SHOULD READ ON THE NEXT RUN.

# ── STRUCK, with the reason ────────────────────────────────────────────────
#  READ, 19 LOCATIONS:  https://www.chsgarrison.com/cash-bids/       # Garrison -- NEW
#  READ, 19 LOCATIONS:  https://chssunprairie.com/grain/cash-bids/   # Bowbells, Minot, Mohall, Norma, Wiley -- NEW. 302s to http://, mind isBrowsablePage
#  READ, 19 LOCATIONS:  https://www.chsbigsky.com/grain/cash-bids    # Havre + Kershaw NEW; Shelby already live
#  READ, 19 LOCATIONS:  https://www.chsmountainwest.com/grain/cash-bids/  # Kalispell -- NEW
#  READ, ALREADY LIVE:  https://www.chsprimeland.com/grain/cash-bids/     # chsprimeland-chsprimeland
#  READ, ALL DUPLICATES: https://www.chsrochester.com/grain/cash-bids/    # all 8 are chsag-* -- note 1 above was right
#  IS www.chsdevilslake.com ON BOTH RUNS, same consent id d643bbbd:  https://centralplainsag.net/
#     (a direct fetch of centralplainsag.net serves Central Plains Ag's own
#      site, so the two disagree -- but chsdevilslake-hannaford is already live
#      and there is no unread board behind either reading. Off the list.)
#  READ AND LIVE:  https://meyerbrothersgrain.com/cash-bids/
#  IS www.chsagservices.com, 14 locations already live:  https://westcentralag.com/
#  IS www.chsagservices.com, 14 locations already live:  https://chsnortherngrain.com/
#  NOT THE BOARD, the wrapper around the iframe:  https://www.agp.com/bids/
#  SECOND ADDRESS FOR GARRISON, /cash-bids/ answered:  https://chsgarrison.com/

# ── SHOULD READ NOW THAT THE SECOND GENERATION IS HANDLED ──────────────────
https://riceland.coop/                                # Riceland -- answered, 71,864 chars, many locations
https://inco-grain.com/                               # Inco Grain -- answered, the whole 2951-char body is the fixture
https://grower.coopelev.com/                          # Cooperative Elevator -- answered, 8,233 chars

# ── STILL UNREAD, and what each one actually said ──────────────────────────
https://www.agp.com/bids/                             # AGP -- see the note below; neither address answers
https://agp.o.bushelsites.com/cash-bids               # AGP's iframe -- 146 requests, NOT ONE to Bushel
https://luckeyfarmers.com/grain-bids/                 # Luckey Farmers -- 40 requests, no Bushel call
https://www.rivercountrycoop.com/grain                # River Country -- loads cash-bids-front-end.xNlkngCm.js and never calls
https://www.gerberandsons.com/future-test             # Gerber & Sons -- loads futures-front-end 2.9.17, the FUTURES widget. Their "Cash Bids" nav points at a futures page; the cash board is somewhere else or is not published

# AGP IS NOT SOLVED BY THE IFRAME. The wrapper made 18 requests and none were
# Bushel; the iframe itself made 146 and none were Bushel either -- it is a
# bushelsites template full of jQuery and bxslider. Whatever serves that board
# is a third address nobody has watched. Both are kept so the next run says so
# with evidence rather than from memory.
#
# LUCKEY, RIVER COUNTRY AND GERBER share one shape: the widget's JS is on the
# page and no request follows. Either the board is server-rendered into the
# markup (check for a table before writing an adapter -- Ace Ethanol did
# exactly this, see the top of this file), or the widget needs an interaction.
# Neither is a reason to call them non-customers.
